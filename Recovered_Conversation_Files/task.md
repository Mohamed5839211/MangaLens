# مهام التنفيذ: إصلاح الترجمة وتنظيف الصور

- `[x]` 1. تحديث `ocr_service.dart` — نظام النقاط (Scoring) + فرض اللغة الصارم + دالة `detectChapterLanguage`.
- `[x]` 2. تحديث `chapter_preview_screen.dart` و `reader_provider.dart` — استشعار اللغة المبكر فور فتح المعاينة.
- `[x]` 3. تحديث `inpainting_service.dart` — تحويل WebP إلى PNG + حل طوارئ Canvas.
- `[x]` 4. تحديث `text_renderer.dart` — إطار حماية أسود (Stroke) حول النص الأبيض.
